import React from 'react';
import ReactDOM from 'react-dom/client';
import ForceGraphCluster from './ForceGraphCluster'; // ✅ YOUR component

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ForceGraphCluster />
  </React.StrictMode>
);
